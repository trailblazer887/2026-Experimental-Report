## 实验要求 
### adder实验
请参考实验指导手册中的第一个实验，将原始的加法器进行功能改进，要求如下：
1、将原始的32位数加法器修改成64位数的加法器。
2、修改仿真文件，用仿真波形验证64位加法器的正确性。
3、修改display文件，从lcd屏上获取两个64位数相加，并将这两个数及相加后的和展示在lcd屏上。（注意lcd屏上一次只能输入一个32位数，所以需要增加片选，保证两个64位数的输入；同时，lcd屏上一格只能显示一个32位数，所以需要用两格显示64位数，分别是高32位和低32位。）修改约束文件，将完整的位流文件烧录到实验箱，在实验箱上操作验证正确性。
4、撰写实验报告，包含实验目标、实验原理图（参考图2.40）、实验过程（修改的代码及注释）、实验结果（波形图和实验箱照片，注意要有对波形数据和实验箱数据的情况说明）以及实验收获感想。报告中原理图参考手册中的图2.40，建议使用visio画图或者其他画图软件（若不熟悉画图软件，可手绘拍照）。实验报告提交pdf格式，建议使用latax写或者word写完后另存成pdf，避免排版混乱。

---

### multiply实验
请结合实验指导手册中的实验二（定点乘法器实验）完成性能改进，不在是原始的最长32个时钟周期完成乘法，注意以下几点：
1、本次主要修改multipler,v模块，建议从两位乘开始进行，此外还有华莱士树等高级优化方式，鼓励大家尝试。
2、实验报告中需要补充原理图，并对原理图进行解释说明。原理图参照图3.2进行修改，建议使用visio画图（别的画图软件也可，不会画图的可以手绘然后照片放报告里面）。
3、撰写实验报告，包含实验目标、实验原理图、实验过程（修改的代码及注释）、实验结果（波形图和实验箱照片，注意要有对波形数据和实验箱数据的情况说明）以及实验收获感想。
4、注意实验报告中要有仿真结果（波形截图），并针对图中的数据解释说明，重点说明乘法结果的正确性和优化前后的时钟周期数情况，还需要有实验箱上箱验证的照片，同样，针对照片中的数据也需要解释说明。
实验报告提交pdf格式，建议使用latax写，避免排版混乱。

---

### regfile实验
请结合实验指导手册中的实验三（寄存器堆实验）完成功能改进，注意以下几点：
1、完成对寄存器写操的改进，原来的写使能（wen）信号扩展成2位，分别控制写入wdata的高16位和低16位。
2、完成寄存器的功能改进，增加一个专用寄存器，用来存储1号寄存器和2号寄存器中数据之和。
3、实验报告中需要补充原理图，并对原理图进行解释说明。原理图参照寄存器设计那个图进行修改，建议使用visio画图。
4、实验报告中不要求有仿真图；需要有实验箱上箱验证的照片，同样，针对照片中的数据及相关读写操作需要解释说明。

---

### 创新实验
CNN核心原理
卷积神经网络可理解为机器的特征放大镜，通过三层图像处理流程逐级筛选关键信息：
1. 卷积层
等效为带特征权重的卷积核在图像上滑动，以像素值与权重相乘累加的方式，强化匹配区域特征、弱化背景信息。
计算示例：
图像局部矩阵：\[[5,10,5],[3,8,3],[2,5,2]]
卷积核：\[[0,1,0],[1,2,1],[0,1,0]]
输出特征值=5×0+10×1+5×0+3×1+8×2+3×1+2×0+5×1+2×0=37
2. 激活层
采用ReLU函数完成无效特征过滤，规则为正数保留、负数置0，仅保留显著有效特征。
示例：输入[-2,35,-5,40]，经ReLU输出[0,35,0,40]。
3. 池化层
以区域均值/最大值生成特征缩略图，在保留特征信息的同时压缩数据量，降低芯片计算压力。
示例：2×2平均池化，输入[20,28,32,40]，输出=(20+28+32+40)/4=30。
边缘端设备芯片面积与功耗受限，32位浮点数运算资源占用高、耗电高；8位定点数可在保证识别精度前提下，显著缩小运算器体积、降低功耗。
本实验面向智能宠物喂食器猫狗面部特征提取需求，设计8位定点卷积‑激活‑池化链运算器，实现边缘端高效计算。
二、总体目标
设计面向边缘端AI的8位定点卷积‑激活‑池化链运算器，模拟智能宠物喂食器猫狗面部特征提取核心流程。
• 输入：5×5 8位无符号整数灰度矩阵（0–255）
• 流程：5×5输入 → 3×3卷积层 → ReLU激活层 → 2×2平均池化层
• 输出：2×2特征值矩阵
• 实现方式：8位定点数，包含乘法器、加法器、比较器、除法器
• 平台：Vivado完成代码编写、功能仿真、综合与时序分析
• 目标：最小化LUT资源占用、最小化计算时延
• 周期：3周 
> 第一周：
核心模块设计与功能验证
• 时间：第1周
• 任务：
a. 编写8位无符号乘法器（卷积乘算）
b. 编写8位加法器（卷积累加、池化求和）
c. 编写ReLU激活模块（无符号数直接输出）
d. 编写除以4除法器（池化平均）
e. 功能仿真验证模块正确性
• 交付物：各模块Verilog代码、仿真波形图

> 第二周：
链式运算器集成与系统仿真
• 时间：第2周
• 任务：
a. 集成乘法器/加法器，实现3×3卷积单元
b. 按流程完成卷积‑激活‑池化全链路系统集成
c. 以标准5×5灰度矩阵完成系统仿真
交付物：全链路Verilog代码、系统仿真波形图、输入输出结果对比表

> 第三周：
优化与Vivado性能验证
• 时间：第3周
• 任务：
a. 依据优化建议完成至少1个模块优化（进位链加法器、移位除法等）
b. Vivado综合（查看LUT）与时序分析（查看时延）
c. 对比优化前后资源与时序数据
交付物：优化后代码、综合报告截图、时序报告截图、性能对比表
---
### alu实验
请结合实验指导手册的实验四，ALU实验，复现并完成功能改进：
1、将原始的ALU操作码，从12位调整成4位，并在display中修改，让通过实验箱上的四个拨码开关来控制ALU的运算（源码中是同lcd屏输入操作码）。
2、4位操作码会有15种运算，请自行补充至少三种其他运算（要求比较预算一种，位运算一种，加载运算一种），并验证正确性。
3、实验报告中重点写明自己修改的部分，实验原理图参考图5.3修改即可。报告中需要有改进之后的波形仿真和上箱验证。

---
### 单周期CPU实验
请结合实验指导手册中的单周期CPU实验，复现体会功能，掌握知识点后完成下列改进：
1、给现有的CPU增加两条MIPS指令，分别是减法指令sub（R型指令）和大于零跳转指令bgtz（I型指令），两条指令指令格式可参考MIPS指令手册或课程附件（单周期CPU改进实验辅助说明材料）。
2、修改inst_rom.v文件，实现使用现有的MIPS指令计算斐波那契数列计算，具体参考课程附件（单周期CPU改进实验辅助说明材料）。
3、实验报告重点说明自己的实现思路，实验原理图把附件中的汇编示意图细化成自己的每条汇编并辅助以注释说明即可。实验箱照片或波形仿真图中应该能看到最终的Fib(10)计算结果。

---
### 多周期/流水线实验
请在复现实验指导手册中的多周期CPU实验和五级流水线CPU两个实验之后，完成下面的改进要求：
1、（基本要求50%）找到多周期和五级流水线CPU原始代码和方案中存在的bug并修复，详细说明bug发现过程和修复过程及效果，保证现有的coe中汇编程序执行正确。
2、（提高要求50%）仿照单周期CPU实验，自行用MIPS汇编写一个完整具体的计算任务，把这个计算任务放到coe中交给流水线CPU执行。注意，在流水线执行过程中会出现数据相关等情况，请自行选择方案解决数据冲突（三个方案，分别是填充空指令、流水线wait阻塞、旁路），把解决冲突的实现过程和效果写入实验报告。
3、开放性实验，大家根据自己能力选择即可。实验原理图用图9.1中的c子图即可。